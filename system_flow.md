# Flow ระบบ “เสียงสารคาม”

## 1. ภาพรวมระบบ

ระบบ “เสียงสารคาม” เป็นแพลตฟอร์มที่เชื่อมโยงลูกค้ากับร้านอาหารท้องถิ่นในจังหวัดมหาสารคาม โดยใช้เสียงเรื่องเล่าจากเจ้าของร้านเป็นส่วนสำคัญในการสร้างประสบการณ์

ผู้ใช้สามารถเลือกใช้งานได้ 2 รูปแบบ:

- ลูกค้า: ค้นหาร้านอาหาร ฟังเรื่องเล่า และบันทึกร้านโปรด
- พ่อค้าแม่ค้า: ดูยอดขาย บันทึกรายรับ-รายจ่าย และดูสรุปการเงิน

นอกจากนี้ยังมีฟีเจอร์เสริมสำหรับแชท AI และสร้างภาพผ่าน AskMe API

---

## 2. Flow หน้าเริ่มต้น

ไฟล์: `testweb.html`

```text
เปิดเว็บไซต์
    ↓
หน้าเลือกบทบาท
    ├── ฉันมาหาของกิน
    │       ↓
    │   customer.html
    │
    └── ฉันเป็นพ่อค้าแม่ค้า
            ↓
        vendor.html
```

หน้าเลือกบทบาทมีองค์ประกอบหลัก:

- โลโก้และชื่อ “เสียงสารคาม”
- คำอธิบายแพลตฟอร์ม
- ปุ่มข้อมูลโครงการ
- ปุ่มเข้าสู่ระบบฝั่งลูกค้า
- ปุ่มเข้าสู่ระบบฝั่งพ่อค้าแม่ค้า

---

## 3. Flow ฝั่งลูกค้า

### 3.1 หน้าหลักลูกค้า

ไฟล์: `customer.html`

ลูกค้าสามารถดำเนินการได้ดังนี้:

- ค้นหาร้านจากชื่อร้าน ประเภทอาหาร หรือเสียงเรื่องเล่า
- ดูรายการร้านยอดนิยม
- ดูร้านใกล้ตัว
- สุ่มร้านอาหาร
- ดูรายละเอียดงานเทศกาล
- กดการ์ดร้านเพื่อดูรายละเอียด
- กลับไปหน้าเลือกบทบาท

```text
customer.html
    ├── ค้นหาร้าน
    │       ↓
    │   กรองรายการร้านแบบทันที
    │
    ├── สุ่มร้าน
    │       ↓
    │   customer-detail.html
    │
    ├── กดการ์ดร้าน
    │       ↓
    │   customer-detail.html
    │
    ├── ร้านใกล้ตัว
    │       ↓
    │   แสดงข้อความแจ้งเตือน
    │
    └── รายละเอียดงานเทศกาล
            ↓
        แสดงข้อความแจ้งเตือน
```

### 3.2 การค้นหาร้าน

ผู้ใช้กรอกคำค้นในช่องค้นหา ระบบจะตรวจสอบข้อมูลต่อไปนี้:

- ชื่อร้าน
- สถานที่ตั้ง
- คำพูดจากเสียงเรื่องเล่า

จากนั้นจะแสดงเฉพาะร้านที่ตรงกับคำค้น

```text
กรอกคำค้น
    ↓
แปลงคำค้นเป็นตัวพิมพ์เล็ก
    ↓
เปรียบเทียบกับข้อมูลร้าน
    ↓
แสดงรายการร้านที่ตรงกัน
```

### 3.3 หน้ารายละเอียดร้าน

ไฟล์: `customer-detail.html`

หน้ารายละเอียดร้านประกอบด้วย:

- รูปภาพร้าน
- ชื่อร้าน
- เวลาเปิด-ปิด
- เสียงเรื่องเล่าจากเจ้าของร้าน
- Progress bar ของเสียง
- จำนวนผู้ฟัง
- จำนวนผู้บันทึกร้านโปรด
- ปุ่มบันทึกร้านโปรด
- รีวิวจากลูกค้า
- ปุ่มย้อนกลับไปหน้าหลักลูกค้า

```text
customer-detail.html
    ├── เล่นเสียงเรื่องเล่า
    │       ↓
    │   เปลี่ยนเป็นปุ่มหยุดชั่วคราว
    │       ↓
    │   Progress bar เดิน
    │
    ├── บันทึกร้านโปรด
    │       ↓
    │   เปลี่ยนสถานะเป็น “บันทึกแล้ว”
    │
    └── ย้อนกลับ
            ↓
        customer.html
```

> ปัจจุบันเสียงเป็น interaction จำลองด้วย JavaScript และ progress bar ยังไม่ได้เชื่อมกับไฟล์เสียงจริง

---

## 4. Flow ฝั่งพ่อค้าแม่ค้า

### 4.1 หน้าหลักร้านค้า

ไฟล์: `vendor.html`

พ่อค้าแม่ค้าสามารถ:

- ดูยอดขายวันนี้
- กดปุ่มบันทึกรายรับ-รายจ่ายด้วยเสียง
- เปิดโหมดอัดเสียงเล่าเรื่องร้าน
- ดูสรุปการเงิน
- กลับไปหน้าเลือกบทบาท

```text
vendor.html
    ├── กดบันทึกรายการด้วยเสียง
    │       ↓
    │   สถานะ “กำลังฟังอยู่”
    │       ↓
    │   กดอีกครั้งเพื่อจบการบันทึก
    │
    ├── อัดเสียงเล่าเรื่องร้าน
    │       ↓
    │   แสดงข้อความแจ้งเตือน
    │
    └── ดูสรุปการเงิน
            ↓
        vendor-finance.html
```

### 4.2 หน้าสรุปการเงิน

ไฟล์: `vendor-finance.html`

แสดงข้อมูล:

- รายรับ
- ต้นทุน
- กำไรสุทธิ
- กราฟยอดขายย้อนหลัง 4 สัปดาห์
- ปุ่มดาวน์โหลดสรุปการเงิน
- ปุ่มย้อนกลับไปหน้า vendor

```text
vendor-finance.html
    ├── ดูรายรับ ต้นทุน และกำไร
    ├── ดูกราฟยอดขาย
    ├── ดาวน์โหลดสรุป
    │       ↓
    │   สร้างไฟล์ .txt
    │
    └── ย้อนกลับ
            ↓
        vendor.html
```

---

## 5. Flow การเชื่อมโยงหน้าเว็บไซต์

```text
testweb.html
├── customer.html
│   └── customer-detail.html
│       └── customer.html
│
└── vendor.html
    └── vendor-finance.html
        └── vendor.html
```

รายละเอียดการเชื่อมโยง:

- `testweb.html` → `customer.html` เมื่อเลือก “ฉันมาหาของกิน”
- `testweb.html` → `vendor.html` เมื่อเลือก “ฉันเป็นพ่อค้าแม่ค้า”
- `customer.html` → `customer-detail.html` เมื่อกดการ์ดร้าน
- `customer.html` → `customer-detail.html` เมื่อกดสุ่มร้าน
- `customer-detail.html` → `customer.html` เมื่อกดย้อนกลับ
- `vendor.html` → `vendor-finance.html` เมื่อกดสรุปการเงิน
- `vendor-finance.html` → `vendor.html` เมื่อกดย้อนกลับ
- ทุกหน้าสามารถกลับไป `testweb.html` ได้ตามปุ่มที่กำหนด

---

## 6. Flow การส่งข้อมูลระหว่างหน้าร้านค้า

การเลือกร้านจาก `customer.html` จะเก็บลำดับร้านไว้ใน `sessionStorage`

```text
ผู้ใช้กดการ์ดร้าน
    ↓
เก็บ selectedStore ใน sessionStorage
    ↓
เปิด customer-detail.html
    ↓
อ่าน selectedStore
    ↓
แสดงข้อมูลร้านที่เลือก
```

ตัวอย่างข้อมูลที่เก็บ:

```javascript
sessionStorage.setItem("selectedStore", "0");
```

หน้า `customer-detail.html` จะอ่านค่าด้วย:

```javascript
const selectedStore = sessionStorage.getItem("selectedStore");
```

> ปัจจุบันข้อมูลร้านยังอยู่ใน JavaScript ภายในไฟล์ HTML แต่ละหน้า ยังไม่ได้ดึงจากฐานข้อมูลจริง

---

## 7. Flow การสร้างภาพ

ไฟล์: `image.html`

ฟีเจอร์นี้ใช้ AskMe API สำหรับสร้างภาพจาก prompt และรูปอ้างอิง

```text
เปิด image.html
    ↓
Login อัตโนมัติ
    ↓
ได้รับ access_token
    ↓
สร้าง chat session
    ↓
เลือกรูปอ้างอิงสูงสุด 2 รูป
    ↓
กรอก prompt
    ↓
เลือกขนาดภาพ
    ↓
กดสร้างภาพ
    ↓
แปลงรูปเป็น raw base64
    ↓
ส่ง prompt + input_images ไป API
    ↓
ได้รับ path หรือ URL ของภาพ
    ↓
normalize URL
    ↓
fetch ภาพพร้อม Authorization header
    ↓
แปลง response เป็น Blob
    ↓
สร้าง Object URL
    ↓
แสดงภาพบนหน้าเว็บ
```

### API ที่เกี่ยวข้อง

#### Login

```text
POST /api/v1/auth/login
```

Body:

```json
{
  "email": "อีเมลผู้ใช้",
  "password": "รหัสผ่าน"
}
```

#### สร้าง Session

```text
POST /api/v1/chat/sessions
```

ต้องส่ง header:

```text
Authorization: Bearer <access_token>
X-Client-Type: web
```

#### สร้างภาพ

```text
POST /api/v1/chat/sessions/{session_id}/images/generate
```

Body:

```json
{
  "prompt": "prompt text",
  "size": "768x1344",
  "model_id": "gemini-3.1-flash-image-preview",
  "input_images": []
}
```

---

## 8. Flow การแชท

ไฟล์: `chat.html`

```text
เปิด chat.html
    ↓
Login อัตโนมัติ
    ↓
ได้รับ access_token
    ↓
โหลดรายการโมเดล
    ↓
ผู้ใช้เลือกโมเดล
    ↓
พิมพ์ข้อความ
    ↓
ส่งคำขอแบบ stream: true
    ↓
อ่านข้อมูลแบบ Server-Sent Events
    ↓
รวมข้อความจาก delta.content
    ↓
แสดงคำตอบแบบต่อเนื่อง
    ↓
เก็บ session_id
    ↓
ใช้ session_id ในข้อความถัดไป
```

### API ที่เกี่ยวข้อง

#### โหลดโมเดล

```text
GET /api/v1/chat-api/models
```

#### ส่งข้อความ

```text
POST /api/v1/chat-api/chat/completions
```

ตัวอย่าง Body:

```json
{
  "model": "ชื่อโมเดล",
  "stream": true,
  "max_tokens": 1024,
  "temperature": 0.7,
  "messages": [
    {
      "role": "user",
      "content": "ข้อความของผู้ใช้"
    }
  ]
}
```

---

## 9. สถานะปัจจุบันของระบบ

### ฟีเจอร์ที่ทำงานแล้ว

- Navigation ระหว่างหน้า
- เลือกบทบาทผู้ใช้
- ค้นหาร้าน
- สุ่มร้าน
- เปิดรายละเอียดร้าน
- ปุ่มเล่นเสียงจำลอง
- บันทึกร้านโปรดแบบจำลอง
- Dashboard ร้านค้า
- ดาวน์โหลดสรุปการเงินเป็น `.txt`
- Chat UI
- Image generation UI
- Login และเรียก AskMe API

### ฟีเจอร์ที่ยังเป็นข้อมูลจำลอง

- รายการร้านอาหาร
- ตำแหน่งร้าน
- คะแนนและรีวิว
- ยอดขาย
- กราฟการเงิน
- เสียงร้านค้า
- ฟังก์ชันแผนที่
- การอัดเสียงจริง
- ระบบบันทึกข้อมูลถาวร
- ระบบร้านโปรดที่แชร์ข้ามอุปกรณ์

---

## 10. สิ่งที่ควรพัฒนาต่อ

1. เชื่อมฐานข้อมูลร้านค้า
2. ใช้ Geolocation และแผนที่จริง
3. เชื่อมไฟล์เสียงจริง
4. ใช้ `MediaRecorder` สำหรับอัดเสียง
5. ทำระบบบัญชีให้บันทึกข้อมูลจริง
6. เพิ่มระบบ authentication สำหรับ vendor
7. ย้าย credentials ออกจาก frontend
8. ทำ backend สำหรับจัดการ API และข้อมูลผู้ใช้
9. เพิ่มระบบ favorites แบบถาวร
10. เพิ่มระบบ upload รูปและเสียงของร้านค้า
11. เพิ่มระบบแจ้งเตือนและสถานะร้านเปิด-ปิดแบบ real-time
12. เพิ่มระบบจัดการร้านสำหรับผู้ดูแลระบบ

---

## 11. โครงสร้างไฟล์หลัก

```text
trainHackathon/
├── testweb.html
├── customer.html
├── customer-detail.html
├── vendor.html
├── vendor-finance.html
├── chat.html
├── image.html
├── sample_prompt.md
├── system_prompt.md
└── system_flow.md
```

---

## 12. หมายเหตุด้านความปลอดภัย

ระบบต้นแบบปัจจุบันมีการเรียก API จาก frontend โดยตรง และอาจมีการฝังข้อมูลเข้าสู่ระบบไว้ในโค้ด ซึ่งไม่ควรใช้ในระบบจริง

เมื่อพัฒนาเป็นระบบจริงควร:

- ย้ายการ Login ไปไว้ที่ backend
- ไม่เก็บอีเมลหรือรหัสผ่านไว้ใน HTML
- ไม่เปิดเผย access token ใน client โดยไม่จำเป็น
- ใช้ session หรือ secure cookie
- เพิ่มการหมดอายุและ refresh token
- ตรวจสอบสิทธิ์ผู้ใช้ทุก endpoint
- จำกัดขนาดและชนิดของไฟล์ upload
- ตรวจสอบและ sanitize ข้อมูลจากผู้ใช้
